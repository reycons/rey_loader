"""rey_loader — CLI operations.

This module is intentionally empty. All stage logic lives in dedicated modules:
  rey_loader.transform  — transform stage
  rey_loader.load       — load stage
  rey_loader.sync       — sync stage
"""
